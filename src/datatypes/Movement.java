package datatypes;

/**
 * This Java file contains the data types for general movement settings.
 */
public enum Movement {
	TURN_LEFT, TURN_RIGHT, MOVE_FORWARD, TURN_RIGHT_TWICE, 
	M, L, R, LR, T, Y, V, Z;
}
